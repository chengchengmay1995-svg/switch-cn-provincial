# pip dependencies for the `switch` conda env
# Install via:  conda env create -f environment.yml
# Or, into an existing Python 3.10 env:  pip install -r requirements.txt

# switch_model 2.0.9 still uses pkg_resources, which was removed in setuptools 81
setuptools<81

# Core model framework (not on conda-forge; pip only)
switch_model==2.0.9.post0
pyomo==6.9.1

# HiGHS solver via Pyomo's appsi_highs interface
# Preferred mode: IPM + run_crossover=off (province-scale LP ~10x faster than dual simplex)
highspy==1.14.0