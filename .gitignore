# compiled Python files
__pycache__
*.pyc

# macOS metadata
.DS_Store
Icon?

# MS Office temp files
~$*

# Switch run artifacts (per-scenario)
scenarios/*/outputs/
scenarios/*/logs/
# Top-level logs/ created by ad-hoc `switch solve` runs
logs/
# results_archive/ is intentionally NOT ignored — selectively commit summaries there
